# Proto repository (Assignment 2 — Repository A)

Push the contents of this directory to a dedicated GitHub repository that contains **only** `.proto` files (contract-first).

Suggested remote name: `ap2-protos` (or similar).

The generated Go module lives in [`../rpcgen`](../rpcgen) (Repository B).
